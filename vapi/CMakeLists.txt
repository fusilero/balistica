#
# This file is a dummy CMakeLists file. It's never actually called from any of
# the other scripts. It's merely here as a placeholder. All the relavent CMake
# code for the VAPIs is in src/CMakeLists.txt where we set the variable
# ${CUSTOM_VAPIS}.
# 
